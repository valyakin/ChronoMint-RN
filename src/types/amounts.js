/* @flow */

import Amount from '../../mint/src/models/Amount'

/**
 * Type created via 'typof Amount'
 */
export type TAmount = typeof Amount

/**
 * Alias for TAmount type
 */
export type TAmountModel = TAmount
